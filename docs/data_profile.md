# Профиль исходных данных

Профиль ещё не сформирован: в переданной рабочей копии отсутствует каталог `data/` с `nodes.parquet`, `edges.parquet` и `transactions.parquet`.

После передачи разрешённого набора разработчик 1 должен записать здесь только агрегированные сведения: SHA-256 и размеры файлов, строки, уникальные gid и seed, распределение depth, период и суммы, пропуски, повторные пары, а также результат сверки `edges` с агрегацией `transactions`. Исходные данные в Git не добавляются.
