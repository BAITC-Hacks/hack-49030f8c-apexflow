# Приёмка интегрированного ApexFlow

Дата: 23 сентября 2026. Этот документ фиксирует выполненные проверки и внешние
ограничения; исходные планы в `docs/team/` сами по себе не означают приёмку.
Точные версии кода, входные хэши и длительность каждого свежего расчёта находятся
в `run_manifest.json` выбранного output. Финальный аудит сохраняет отдельные
результаты в `.ci/readiness/final-output`, не перезаписывая прежний `output/`.
Реальные CSV и Parquet в Git не входят.

## Окружение

- Хост: Apple M2, 8 CPU, 16 GiB RAM, Docker Desktop 29.8.0.
- Docker VM: aarch64, 8 CPU, 8 319 770 624 байт RAM.
- Runtime: Python 3.12, pandas 2.2.3, NetworkX 3.4.2, PyArrow 18.1.0,
  Streamlit 1.41.1; точная версия Python записывается в manifest.
- Прямые и транзитивные зависимости закреплены в `requirements*.txt`, образ —
  по digest в Dockerfile; первоначальная сборка
  требует доступа к реестру образов и пакетам. Анализ после сборки выполняется
  с `network_mode: none`.

## Воспроизводимые проверки

```bash
export APEXFLOW_REVISION=$(git rev-parse HEAD)
docker compose --profile test run --rm --build tests
docker compose build pipeline ui
docker compose run --rm pipeline python -m scripts.verify_dataset
docker compose up --force-recreate --wait --wait-timeout 120 ui
docker compose exec -T ui python -m scripts.ci_smoke verify /app/data /app/output
docker compose exec -T ui python -m ui.demo_examples
```

`scripts.verify_dataset` повторяет полный расчёт, проверяет байты трёх CSV,
равенство после перестановки строк, все gid в загрузчике UI, изоляты, ограничения
seed/depth=4 и неизменность исходников. Он печатает агрегированный JSON-отчёт.
Это локальный прогон реальных данных, а не замена браузерной проверки.

## Финальная интеграция актуальных работ

Проверен commit `91ead099acd7d0556514ba9135f2986195275c74`: исправления этого
аудита, свежая staging `fa4f787` (pipeline PR #13) и UI из main `e8d9c43`
(внешний PR #14). Слияния выполнены в личной ветке; main этим аудитом не меняется.
Последующие правки документации не меняют проверенный исполняемый код.

- Сборка по `requirements-lock.txt` и закреплённому Python digest успешна;
  `pip check` не обнаружил несовместимых зависимостей.
- Полный `tests/compose_smoke.sh` завершился с exit 0: **171 Python-тест за
  13,81 с**, один прежний NumPy DeprecationWarning; реальный CLI на synthetic
  файлах, UI health, запрет записи в read-only тома, повторяемость, сохранение
  после down, отказ при отсутствии Parquet и восстановление повторным расчётом.
- В закреплённом Node 22 образе CI прошли **6 тестов** и проверка синтаксиса.
- Реальный `scripts.verify_dataset` прошёл полностью: **0,730951500 и
  0,751751958 с** вокруг полного pipeline, включая финальные метаданные.
  Сборка и старт контейнера не входят. Получены прежние 2 248 ролей,
  88 кластеров и топ-20; все gid и 19 изолятов сохранены.
- Три CSV совпали побайтово с предыдущим output. Перестановка строк,
  неизменность DataFrame, полнота поискового набора, ограничения seed/depth=4
  и буферы скачивания проверены повторно.
- Новые `--acceptance --json` и `--verify-output` выполнены на полном наборе
  успешно; проверка кода и статуса проходит для нового output.
- Полный Compose с новым образом поднял UI healthy на отдельном порту 8512.
  Исходники подключены read-only. Все 13 исходных PDF/Parquet/сохранённых CSV
  остались с прежними SHA256. В 18 Markdown-файлах нет битых локальных ссылок.

Локальный лог полного smoke — `.ci/readiness/final-smoke.log`, реальный
машинный отчёт — `.ci/readiness/final-dataset-report.json`. Эти файлы, реальные
данные и результаты не включены в Git. Документированная разработчиком 1
проверка Windows/amd64 сохранена в [профиле данных](data_profile.md);
выше — собственная повторная проверка объединённой версии на macOS/arm64.

## Проверка исправлений до обновления staging

Повторно проверен код commit `7b657aa19de271db82b3f55eff88dfa45fdffeb2`
в собственной ветке от `staging`. Прямо прочитаны исходные PDF и сверены
все группы файлов; таблица требований и 11 пунктов README — в
[requirements-audit.md](requirements-audit.md).

| Проверка после исправлений | Результат |
|---|---|
| Полный test-target Compose | **144 passed**, 5,21 с, без сети; один прежний NumPy DeprecationWarning в тестовой фикстуре, падений и skip нет |
| Статический сайт в Node 22 | **6 passed** без сети; `node --check site/app.js` успешен |
| Сборка runtime pipeline/UI | Успешна; в образе нет CSV, Parquet, `.env`, ключей и `.DS_Store`; UID 10001 |
| Полный реальный pipeline, два запуска | **0,563574209 и 0,562883584 с**, включая финальную запись manifest; старт/сборка контейнера отдельно |
| Результаты | 2 248 ролей, 88 кластеров, топ-20; все 2 248 gid доступны, все 19 изолятов сохранены |
| Детерминизм | Три CSV совпали побайтово между запусками и с предыдущими сохранёнными CSV; перестановка входных строк не меняет таблицы |
| Полный Compose → UI | Pipeline exit 0, UI healthy; smoke: завершённый manifest, поиск всех gid, HTTP 200 |
| Браузер нового образа | Реальные сводные числа, поиск полного seed gid, корректная оговорка о входящих извне графа за тот же период и следующий шаг |
| Сохранность файлов | Все 13 ранее существовавших PDF/Parquet/CSV сохранили SHA256; исходный output и CSV в корне не перезаписаны |
| Документация | 17 Markdown-файлов проверены на существование локальных ссылок; `git diff --check` успешен |

Проверка выполнялась без bind-mount исходного кода, на Python 3.12.14,
pandas 2.2.3, PyArrow 18.1.0, NetworkX 3.4.2. Повторный расчёт направлен в
отдельный `.ci/readiness/output`; временный Compose-проект использовал локальный
порт 8512, чтобы не остановить основной интерфейс и не изменить старые файлы.
Служебные JSON/PNG и результаты аудита находятся в игнорируемой `.ci/readiness/`.
Точные CSV-хэши совпадают с [профилем данных](data_profile.md).

Исправлены Linux-порядок подготовки output, объяснение неполных входящих seed,
точная сверка больших целочисленных сумм и устаревшие сведения в документации.
Общие сценарии браузера (boundary, изолят, граф, фильтры, скачивание) также
повторно проверены на исходной staging до этих изменений; их более ранний
протокол сохранён ниже. Текущий аудит не подтверждает запуск другим человеком,
проверку на отдельной Linux-машине или отправку решения на платформу.

## Предыдущая интеграционная проверка

Интегрированная версия `9e6e21c` прошла 141 Python-тест за 5,27 с в test-образе
без сети и 6 тестов статического демо в `node:22-alpine` без сети. `node --check`
и `git diff --check` также завершились успешно. В pytest был один
DeprecationWarning NumPy о сложении даты в тестовой фикстуре; падений и skip нет.

На реальных данных `scripts.verify_dataset` подтвердил:

| Проверка | Фактический результат |
|---|---|
| Полнота | 2 248 узлов, 3 119 рёбер, 4 840 транзакций; все gid доступны поиску |
| Изоляты | Все 19 сохранены; каждый рисуется единственным узлом |
| Выходы | 2 248 строк ролей, 88 кластеров, топ-20 |
| Полный pipeline | 0,564239834 и 0,562163584 с, измерено вокруг всего вызова, включая manifest |
| Повторный расчёт | Побайтово одинаковые три CSV |
| Перестановка строк | Точные таблицы ролей, кластеров и топа совпали |
| Сохранность | SHA256 входов не изменились; analyze не изменил переданные DataFrame |
| Выгрузки | Все три буфера загрузчика UI побайтово совпали с файлами |
| Ограничения | Seed/depth=4 не назначены terminal/transit |

Это два запуска в уже собранном контейнере с установленными зависимостями;
время скачивания, сборки и старта контейнера в измерения не включено. Хэши
CSV совпали с [профилем данных](data_profile.md). Python runtime — 3.12.14.
Сборка выполнена из основной рабочей папки пользователя, где находились старые
CSV в корне: внутри готового образа проверено отсутствие любых Parquet и CSV.
Корневые экспорты исключены через `.dockerignore` и `.gitignore`.

Штатный Compose выполнил pipeline и перевёл UI в `healthy`. В дополнительной
проверке сеть UI была `internal: true`: runtime smoke подтвердил HTTP 200 внутри
контейнера, завершённый manifest и все 2 248 gid. Docker Desktop не предоставил
доступ с хоста к этой внутренней сети, поэтому браузерная проверка ниже проведена
в штатной сети Compose. Рендерер и Streamlit AppTest отдельно прошли без сети.
Отключение сети всего компьютера в браузерном сеансе не выполнялось.

В браузере на реальных результатах проверены поиск полного int64, карточка
приоритетного non-seed, предупреждение и следующий шаг depth=4, изолированный
seed, синхронизация выбранного кластера, поиск при пустом топе с фильтром роли,
ошибка нечислового gid и три события скачивания CSV. Период отображается
01.07.2026–31.07.2026. Направление и сумма наиболее крупного исходящего ребра
сверены с таблицей переводов. Синтетический режим при ошибках автоматически
не включается.

После визуальной проверки плотного графа устранены перекрытия длинных gid:
выбранный клиент находится в центре окружения, метки `№` обозначают локальные
номера, полный gid доступен в подсказке. Размеры узлов учитывают плотность и
обводки seed. Соседи со скрытым ребром не показываются как ложные изоляты;
настоящие изоляты и состав кластеров сохраняются. Две регрессии входят в 141 тест.

Ошибочный вход и неуспешный пересчёт проверены регрессиями; отдельный Compose
прогон с отсутствующими Parquet вернул exit 1, UI не был запущен. После остановки
и повторного создания контейнеров исходники и CSV остались на хосте.

## Выполнение зон 1 и 3

| Задачи из рабочей карты | Статус |
|---|---|
| A1–A7: Docker, ввод/вывод, контракты, реальная аналитика и зависимости сервисов | Реализованы и проверены локально в Docker |
| A8: контейнерный CI | Реализован; удалённое выполнение блокирует GitHub billing |
| A9: независимое воспроизведение и выпуск | Проверка другим человеком и подтверждённый успешным CI релиз остаются открытыми |
| U1–U6: UI, поиск, граф, реальные файлы, банковская карточка, CSV | Реализованы; контейнерные и браузерные проверки выполнены |
| U7: README и схема | Готовы; самостоятельный запуск коллегой ещё не подтверждён |
| U8: защита и сдача | Сценарий и [текст формы](submission.md) готовы; командная репетиция и отправка не подтверждены |

Изменения выполнены агентом по поручению Максима. Это не подтверждает личный
вклад двух других участников. Ветки основаны на `staging`; pipeline включает
уже существующие компоненты из `main` и исправления аналитики PR #7.
Изменения аналитики, pipeline и UI объединены в `staging` через PR #7, #8 и #9
(интеграционный commit `8b70824`). Внешние PR #10–12 также перенесли тот же набор
файлов в `main` (`2cf82cc`); одинаковое содержимое подтверждено сравнением Git
tree, но успешный CI-релиз этим не подтверждён. Итоговый аудит выполняется в
`codex/dev-maxim-readiness` с последующим включением новой staging PR #13
и внешнего UI PR #14. Детальная сверка исходных
PDF, файлов и готовности артефактов — [requirements-audit.md](requirements-audit.md).

## CI и выпуск

`container-ci` выполняет четыре проверки: `release-source`, `static-demo`,
`compose-and-tests`, `runtime-smoke`. Последняя генерирует явно синтетические
Parquet внутри образа, запускает настоящий CLI и зависимый UI, проверяет хэши,
поиск всех клиентов и HTTP readiness. CI не загружает реальные банковские
Parquet; полный реальный прогон проводится отдельно в разрешённом окружении.

PR в `main` допускается только из `staging` этого репозитория. Этот CI-контроль
не заменяет техническую защиту веток. 23 сентября GitHub API показал
`protected: false` у `main` и `staging`; страница Settings текущего аккаунта
показала `You don't have access to repository options`. Настройки защиты не
изменены. Администратору нужно включить PR, одобрение другого участника,
актуальные обязательные проверки выше и запрет force push для обеих веток.

Внешний блокер повторно подтверждён на интегрированной staging `8b70824`:
[run 35857662073](https://github.com/BAITC-Hacks/hack-49030f8c-apexflow/actions/runs/35857662073)
завершился с отказом всех четырёх заданий до старта шагов. В аннотациях GitHub: `The job was not started because
your account is locked due to a billing issue.` Устранение биллинга требует
владельца аккаунта/организации. Локальные Docker-проверки не выдаются за зелёный
Actions. Выпуск через `staging → main` должен дождаться настоящих обязательных
проверок; отключать их или подставлять успешные статусы нельзя.

На дату аудита GitHub API также вернул `has_pages: false` и пустой homepage.
Поэтому публичная развёрнутая версия не заявляется; локальный Docker-продукт
проверен. Workflow Pages сам по себе не является подтверждением deployment.

## Действия с участием команды

- Другой участник должен самостоятельно повторить README: проверки агентом
  не являются подтверждением запуска другим человеком.
- Пятиминутная командная репетиция и личное участие на площадке требуют команды.
  Подготовленный сценарий — [demo.md](demo.md).
- Сдача на платформе проверяется отдельно по фактическому подтверждению.
  Коммит, PR, зелёный CI и локальные CSV не означают, что решение уже отправлено.
